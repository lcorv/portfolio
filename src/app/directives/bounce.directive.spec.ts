import { BounceDirective } from './bounce.directive';

describe('BounceDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceDirective();
    expect(directive).toBeTruthy();
  });
});
